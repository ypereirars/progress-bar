from setuptools import setup

setup(
   name='progress-bar',
   version='0.0.1',
   description='A progress bar for training models',
   author='Yuri Silveira',
   author_email='ypereirars@gmail.com',
   packages=['progress-bar']
)